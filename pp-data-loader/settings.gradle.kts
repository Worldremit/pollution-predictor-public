pluginManagement {
    repositories {
        gradlePluginPortal()
        jcenter()
        maven { url = uri("https://dl.bintray.com/gradle/gradle-plugins") }
    }
}
rootProject.name = "pp-data-loader"
